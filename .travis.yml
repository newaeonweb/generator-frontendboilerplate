language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frontendboilerplate' ]; then cd .. && eval "mv $currentfolder generator-frontendboilerplate" && cd generator-frontendboilerplate; fi
